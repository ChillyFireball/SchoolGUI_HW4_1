Link: 

Notes: Everything that could be done with regular rules uses regular rules. There are also
two custom validation rules for lessThan and greaterThan that ensure max inputs aren't less
than min inputs and vice versa.